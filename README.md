To have this website served from your webserver, ensure there is only 1x "A" record named "toetsmy.co.za" that points to your webserver's IP address.

To have this website served from GitHub Pages, ensure there are 4x "A" records named "toetsmy.co.za" that points to 185.199.[108,109,110,111].153. This repo needs to be "public" else it doesn't get served.

Make the change in the Cloudflare DNS. Ensure that each record is "Proxied" for instantaneous results.
